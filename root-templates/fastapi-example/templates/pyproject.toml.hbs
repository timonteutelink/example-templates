[project]
name = "{{package_name}}"
version = "{{package_version}}"
description = "{{package_description}}"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi[standard]>=0.112.1"]

[project.scripts]
{{project_name}} = "app:main"
